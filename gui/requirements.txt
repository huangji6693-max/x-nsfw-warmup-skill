# GUI 模式依赖（在根 requirements.txt 之上增量）
#
# 安装:
#     pip install -r gui/requirements.txt
#
# 所有版本已锁死 —— 防止上游被投毒/行为漂移

# --- 从根 requirements.txt 同步 ---
playwright==1.49.1
requests==2.32.3
python-dotenv==1.0.1

# --- GUI-only ---
nicegui==2.7.0
