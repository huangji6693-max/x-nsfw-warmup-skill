# X NSFW Warmup Skill — docker-compose
#
# Quick start:
#   cp deploy/.env.example .env && nano .env   # 填好敏感配置
#   docker compose -f deploy/docker-compose.yml up -d
#   docker compose -f deploy/docker-compose.yml logs -f warmup
#
# Stop:
#   docker compose -f deploy/docker-compose.yml down

services:
  # ---------------------------------------------------------------------------
  # 主服务：养号循环
  # ---------------------------------------------------------------------------
  warmup:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    image: x-warmup:latest
    container_name: x-warmup
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      # 默认启动 live 模式 —— 需要 .env 里先 explicit 打开 WARMUP_MODE=live
      WARMUP_MODE: ${WARMUP_MODE:-dry-run}
    volumes:
      - ../data:/app/data
      - ../logs:/app/logs
      - ../screenshots:/app/screenshots
      # cookies / 账号池配置挂进来
      - ../cookies:/app/cookies:ro
    command: ["python", "scripts/run_live.py"]
    healthcheck:
      test: ["CMD", "python", "examples/05-full-warmup-loop.py", "--dry-run"]
      interval: 5m
      timeout: 60s
      retries: 3
      start_period: 30s
    # 资源限制 —— 避免 Playwright 吃爆宿主机
    mem_limit: 4g
    mem_reservation: 2g
    cpus: 2.0
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "10"
    networks:
      - warmup-net
    depends_on:
      adspower-bridge:
        condition: service_started

  # ---------------------------------------------------------------------------
  # AdsPower Local API 桥接 —— 如果 AdsPower 装在宿主机上，用这个转发
  # 如果用 patchright 方案（不要 AdsPower），把这段删掉
  # ---------------------------------------------------------------------------
  adspower-bridge:
    image: alpine/socat:latest
    container_name: adspower-bridge
    restart: unless-stopped
    command:
      - TCP-LISTEN:50325,fork,reuseaddr
      - TCP:host.docker.internal:50325
    networks:
      - warmup-net
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # ---------------------------------------------------------------------------
  # 注：已移除 Watchtower 自动更新服务
  # 原因：挂载 /var/run/docker.sock 等同把宿主机 root 权限交给容器，
  #       一旦 watchtower 或其依赖被投毒，整台机器即可被接管。
  # 如需更新镜像：docker compose pull && docker compose up -d
  # ---------------------------------------------------------------------------

networks:
  warmup-net:
    driver: bridge
